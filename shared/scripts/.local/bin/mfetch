#!/bin/sh
printf "\033[1m$(whoami)@$(hostname)\033[0m"

BATTERY='/org/freedesktop/UPower/devices/battery_BAT1'
if upower -d | grep "$BATTERY"; then
	printf " with %s" "$(upower -i "$BATTERY" | awk '/percentage/{print $2}')"
fi
printf '\n'

fortune | fmt -80 -s
